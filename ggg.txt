🔗 تحلیل گراف با NetworkX:  ساختار گراف

این نوت‌بوک به ساخت و تحلیل یک گراف ساده و بدون جهت با استفاده از کتابخانه‌ی قدرتمند networkx می‌پردازد. تمرکز این تحلیل بر ویژگی‌های ساختاری گراف است. 

۱. 🛠️ راه‌اندازی و ساخت گراف اولیه
این بخش شامل تعریف اولیه گره‌ها (Nodes/Vertices)،  یال‌ها (Edges) و نمایش گراف با استفاده از networkx است.

import networkx as nx

# --- (توضیحات Markdown برای سلول کد زیر) ---
## تعریف گراف و افزودن گره‌ها و یال‌ها

# یک شیء گراف خالی *ساده* و بدون جهت (Undirected Graph) ایجاد می‌کند.

# در این نوع گراف، یال‌ها جهت ندارند (مثلاً a-g همان g-a است).

G = nx.Graph()

# تعریف مجموعه گره‌ها (Vertices):

V = list('abcdefg')

# تعریف مجموعه یال‌ها (Edges) به صورت تاپل‌های زوجی:

E = [('a','g'),('a','e'),('b','f'),('e','c'),('a','d'),('f','d')]

# اضافه کردن گره‌ها و یال‌های تعریف‌شده به شیء گراف
G.add_nodes_from(V)
G.add_edges_from(E)

# ترسیم گراف با نمایش برچسب‌های گره‌ها

nx.draw(G, with_labels=True)

۲. 🔄 ویرایش گراف: افزودن گره و یال جدید
در این بخش، یک گره جدید اضافه شده و سپس به شبکه موجود متصل می‌شود.
# --- (توضیحات Markdown برای سلول کد زیر) ---
## افزودن گره منزوی ('z') و ترسیم مجدد

# یک گره جدید به نام 'z' به گراف اضافه می‌شود.
# در این مرحله، 'z' یک گره منزوی (Isolated Node) است.
G.add_node('z')

# ترسیم مجدد گراف با تنظیمات بصری بیشتر
# (اندازه 800، رنگ گره قرمز، اندازه فونت 25 و رنگ فونت سبز)
nx.draw(G,with_labels=True,node_size=800,node_color='red',font_size='25',font_color='green')
# 

# --- (توضیحات Markdown برای سلول کد زیر) ---
## اتصال گره جدید با افزودن یال ('z', 'a')

# یک یال جدید بین 'z' و 'a' اضافه می‌شود.
# اکنون گره 'z' به شبکه متصل شده است.
G.add_edge('z','a')

# ترسیم نهایی گراف
nx.draw(G,with_labels=True,node_size=800,node_color='red',font_size='25',font_color='green')
# 

۳. 📐 محاسبات اولیه ساختاری
این توابع به محاسبه‌ی ویژگی‌های اساسی ساختاری در نظریه گراف می‌پردازند: مرتبه (تعداد گره‌ها) و اندازه (تعداد یال‌ها).
# --- (توضیحات Markdown برای سلول کد زیر) ---
## محاسبه مرتبه (Order) گراف

# مرتبه گراف، تعداد گره‌ها (Vertices) را برمی‌گرداند.
# 7 گره اولیه + 1 گره جدید ('z') = 8
G.order()

# --- (توضیحات Markdown برای سلول کد زیر) ---
## محاسبه اندازه (Size) گراف

# اندازه گراف، تعداد یال‌ها (Edges) را برمی‌گرداند.
# 6 یال اولیه + 1 یال جدید ('z', 'a') = 7
G.size()

# --- (توضیحات Markdown برای سلول کد زیر) ---
## معادل order()

G.number_of_nodes()

# --- (توضیحات Markdown برای سلول کد زیر) ---
## معادل size()

G.number_of_edges()


۴. 📊 تحلیل درجه گره‌ها (Degree Analysis)
درجه‌ی یک گره، تعداد یال‌های متصل به آن گره است. این معیار، یک سنجش محلی برای مرکزیت در شبکه است.

# --- (توضیحات Markdown برای سلول کد زیر) ---
## درجه‌ی یک گره‌ی خاص

# محاسبه‌ی درجه‌ی گره‌ی 'b'
G.degree('b')
# نتیجه: 1 (تنها به 'f' متصل است)

# --- (توضیحات Markdown برای سلول کد زیر) ---
## فهرست گره‌ها و یال‌ها

list(G.nodes())

# --- (توضیحات Markdown برای سلول کد زیر) ---

list(G.edges())

# --- (توضیحات Markdown برای سلول کد زیر) ---
## نمایش درجات برای همه‌ی گره‌ها (به صورت تکرارشونده)

# پیمایش بر روی همه‌ی گره‌ها و چاپ درجه‌ی هر کدام
for v in G.nodes():
    print( G.degree(v) )
# خروجی: 4, 1, 1, 2, 2, 2, 1, 1 (به ترتیب a, b, c, d, e, f, g, z)

# --- (توضیحات Markdown برای سلول کد زیر) ---
## نمایش درجات گره‌ها با برچسب (با استفاده از f-string)

for v in G.nodes():
    print( f' the degree of {v} is {G.degree(v)}' )

# --- (توضیحات Markdown برای سلول کد زیر) ---
## محاسبه‌ی درجات به صورت دیکشنری (DegreeView)

# این متد، درجات تمام گره‌ها را در قالب یک شیء دیکشنری مانند برمی‌گرداند.
nx.degree(G)

# خروجی: DegreeView({'a': 4, 'b': 1, 'c': 1, 'd': 2, 'e': 2, 'f': 2, 'g': 1, 'z': 1})

# --- (توضیحات Markdown برای سلول کد زیر) ---
## ذخیره درجه گره‌ها در یک دیکشنری استاندارد

# تبدیل DegreeView به دیکشنری پایتون
deg = dict(nx.degree(G))

استخراج کلیدها، مقادیر و زوج‌ها
# --- (توضیحات Markdown برای سلول کد زیر) ---
## کلیدهای دیکشنری (نام گره‌ها)

dict(nx.degree(G)).keys()

# --- (توضیحات Markdown برای سلول کد زیر) ---
## مقادیر دیکشنری (درجات گره‌ها)

dict(nx.degree(G)).values()

# --- (توضیحات Markdown برای سلول کد زیر) ---
## زوج‌های گره و درجه (آیتم‌های دیکشنری)

dict(nx.degree(G)).items()

اثبات قضیه‌ی دست دادن (Handshaking Lemma)
# --- (توضیحات Markdown برای سلول کد زیر) ---
## بررسی قضیه دست دادن در نظریه گراف

# قضیه‌ی دست دادن می‌گوید: مجموع درجات گره‌ها (سمت چپ) = 2 * تعداد یال‌ها (سمت راست).
# این قضیه برای هر گراف بدون جهت همواره صحیح است.
sum(dict(nx.degree(G)).values()) == 2 * G.size()
# نتیجه: True

۵. 🗃️ مرتب‌سازی بر اساس درجه
مرتب‌سازی گره‌ها بر اساس درجه (از زیاد به کم) روشی سریع برای شناسایی هاب‌ها (Hubs) یا گره‌های پراتصال در شبکه است.
# --- (توضیحات Markdown برای سلول کد زیر) ---
## مرتب‌سازی صعودی بر اساس درجه (مرتب‌سازی بر اساس مقدار)

# lambda x:x[1] 
به تابع sorted می‌گوید که آیتم دوم هر زوج (یعنی درجه) را برای مرتب‌سازی ملاک قرار دهد.

# reverse=False 
مرتب‌سازی صعودی (کوچک به بزرگ) را انجام می‌دهد.

deg_s=dict(sorted(deg.items(), key=lambda x:x[1]),reverse=False)

# (سلول‌های 20 تا 25، عملیات میانی مرتب‌سازی و نمایش نتایج هستند.)

# --- (توضیحات Markdown برای سلول کد زیر) ---
## نمایش دیکشنری مرتب شده صعودی (بر اساس درجه)

deg_s

# --- (توضیحات Markdown برای سلول کد زیر) ---
## مرتب‌سازی نزولی بر اساس درجه

# reverse=True 
مرتب‌سازی نزولی (بزرگ به کوچک) را انجام می‌دهد.
deg_n = dict(sorted(deg.items(), key=lambda x:x[1]),reverse=True)

# --- (توضیحات Markdown برای سلول کد زیر) ---
## نمایش دیکشنری مرتب شده نزولی (بر اساس درجه)

deg_n
# نتیجه: گره 'a' با درجه 4 در بالاترین رتبه قرار دارد.